# Specification migration

The canonical qpg contract now lives in [docs/spec.md](../docs/spec.md).

The former numbered specification files were consolidated there so behavioral requirements have one
source of truth. Keep `README.md`, `docs/architecture.md`, `docs/cli.md`, `docs/mcp.md`, and related
usage documents aligned by linking to the canonical sections rather than copying contract language.
