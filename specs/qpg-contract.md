# Canonical contract location

The qpg behavioral contract was consolidated into [docs/spec.md](../docs/spec.md).

This file is intentionally only a migration pointer. It MUST NOT contain duplicated behavioral
requirements; edit `docs/spec.md` when changing the contract.
